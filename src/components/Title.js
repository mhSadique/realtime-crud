import { Typography } from '@mui/material';
import React from 'react';

const Title = () => {
    return (
        <div>
            <Typography component='h1' variant='h4'>
                CRUD Realtime system
            </Typography>
        </div>
    );
};

export default Title;